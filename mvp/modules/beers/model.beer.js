// model.beer.js
const SCHEMA = require('./../../schemas/schema.beer');
const MODEL = require('./../../models/model.mongoose')(SCHEMA);

Model = require('./../../models/model')(MODEL);

module.exports = Model;
